# ============================================================
# KRAKEN API CREDENTIALS (REQUIRED)
# ============================================================
# SECURITY WARNING: NEVER commit this file with real values!
# 1. Copy this file to .env
# 2. Fill in your actual API keys
# 3. Verify .env is in .gitignore
# ============================================================

# PRIMARY API KEY (Main trading operations)
# Generate at: https://www.kraken.com/u/security/api
# Permissions needed: Query Funds, Query Open Orders, Create & Modify Orders, Cancel/Close Orders
KRAKEN_API_KEY=your_primary_api_key_here
KRAKEN_API_SECRET=your_primary_api_secret_here

# SECONDARY API KEY (Backup for rate limit distribution)
# OPTIONAL: Leave empty if you only have one key pair
KRAKEN_API_KEY_2=
KRAKEN_API_SECRET_2=

# TERTIARY API KEY (Additional backup)
# OPTIONAL: Leave empty if not needed
KRAKEN_API_KEY_3=
KRAKEN_API_SECRET_3=

# WebSocket Token (Optional - auto-generated if not provided)
KRAKEN_WS_TOKEN=

# Nonce Management (Prevents replay attacks)
# Higher values = more tolerance for clock drift
# Recommended: 10000 (default)
KRAKEN_NONCE_WINDOW=10000

# ============================================================
# TRADING CONFIGURATION
# ============================================================

# Trading pairs (comma-separated, V2 format: BTC/USD, XLM/USD)
TRADING_PAIRS=XLM/USD

# Base currency for calculations
BASE_CURRENCY=USD

# ============================================================
# RISK MANAGEMENT (CRITICAL - LIVE MONEY PROTECTION)
# ============================================================

# Maximum USD value per single position
# PRODUCTION: Start conservative (e.g., $10-30)
MAX_POSITION_SIZE=10

# Maximum total USD exposure across all positions
# PRODUCTION: 3x MAX_POSITION_SIZE for diversification
MAX_TOTAL_EXPOSURE=30

# Maximum number of concurrent positions
# PRODUCTION: Start with 1-3 positions
MAX_POSITIONS=1

# Maximum risk score (0.0 - 1.0)
# Above this threshold, system reduces exposure
MAX_RISK_SCORE=0.6

# Minimum balance alert threshold (USD)
# System logs WARNING when balance falls below this
MIN_BALANCE_ALERT=50

# Minimum required balance to continue trading (USD)
# System STOPS trading when balance falls below this
# CRITICAL: Must be >= (MAX_POSITION_SIZE * 1.5) for safety margin
MIN_BALANCE_REQUIRED=15

# ============================================================
# SYSTEM CONFIGURATION
# ============================================================

# Engine loop interval (seconds)
# How often the trading engine evaluates strategies
# PRODUCTION: 30-60 seconds for conservative trading
ENGINE_LOOP_INTERVAL=30

# Database path
DB_PATH=trading.db

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# PRODUCTION: INFO (DEBUG for troubleshooting)
LOG_LEVEL=INFO

# ============================================================
# XLM-SPECIFIC SETTINGS (If trading XLM/USD)
# ============================================================

# Minimum order size in XLM (Kraken minimum: 20 XLM)
XLM_MIN_ORDER_SIZE=20

# Acceptable price range for XLM (USD)
# Orders outside this range will be rejected
XLM_PRICE_RANGE_MIN=0.33
XLM_PRICE_RANGE_MAX=0.43

# Stop-loss price (USD) - Auto-applied if not specified
XLM_STOP_LOSS_PRICE=0.35

# Take-profit price (USD) - Auto-applied if not specified
XLM_TAKE_PROFIT_PRICE=0.41

# Cooldown period between XLM trades (minutes)
# Prevents rapid-fire trading
XLM_COOLDOWN_MINUTES=5

# ============================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================
# [ ] Copy this file to .env with your actual API keys
# [ ] Verify .env is in .gitignore
# [ ] Test API keys with check_status.py
# [ ] Set conservative risk limits (start small!)
# [ ] Review trading_config.json for strategy parameters
# [ ] Run validation: python final_system_check.py
# [ ] Monitor first few trades manually
# [ ] Review logs regularly: tail -f trading_new.log
# ============================================================
